# Which model at which reasoning effort. One "model effort" pair per line.
#
# The two luna cells that scored full marks in 01-full-grid, plus luna max,
# added after the first two had run, to see whether the top effort stops the
# four letters flipping. terra is ten times the price and is not what the
# reading step currently uses.

gpt-5.6-luna   medium
gpt-5.6-luna   xhigh
gpt-5.6-luna   max
