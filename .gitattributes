# Synthetic letter images for the extraction experiments. Large binaries go
# through Git LFS so the clone stays small; teammates need git-lfs installed.
experiments/**/*.png filter=lfs diff=lfs merge=lfs -text
